import { CustomFormFieldErrors } from './form-field-errors';

export const CUSTOM_FIELD_ERRORS: Record<string, CustomFormFieldErrors> = {
  password: {
    passwordMismatch: 'The password does not match',
  },
};
